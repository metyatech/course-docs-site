## course selection (recommended)
# - Keep secrets in `.env.local`
# - Put only course selection in `.env.course.local`
# - `npm run dev` watches `.env.course.local` and restarts + re-syncs content when it changes

# Use a local course content repo (fast iteration):
COURSE_CONTENT_DIR=../programming-course-docs

# Or, use a remote repo (when COURSE_CONTENT_DIR is empty):
# COURSE_CONTENT_REPO=metyatech/programming-course-docs
# COURSE_CONTENT_REF=master
