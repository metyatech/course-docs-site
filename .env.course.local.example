## course selection (recommended)
# - Keep secrets in `.env.local`
# - Put only course selection in `.env.course.local`
# - `npm run dev` watches `.env.course.local` and restarts + re-syncs content when it changes

# Content source (single variable):
# - GitHub: github:owner/repo#ref
# - Local path: ../path-to-content-repo
COURSE_CONTENT_SOURCE=../programming-course-docs

# Remote example:
# COURSE_CONTENT_SOURCE="github:metyatech/programming-course-docs#master"
