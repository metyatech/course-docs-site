import type { ReactNode } from 'react';

/** Marks the problem-solving phase for deterministic learning-pattern validation. */
export default function ProblemSolving({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
