diff --git a/node_modules/@metyatech/course-docs-e2e/tests/e2e/course-docs-smoke.spec.cjs b/node_modules/@metyatech/course-docs-e2e/tests/e2e/course-docs-smoke.spec.cjs
index 233ede8..2b89821 100644
--- a/node_modules/@metyatech/course-docs-e2e/tests/e2e/course-docs-smoke.spec.cjs
+++ b/node_modules/@metyatech/course-docs-e2e/tests/e2e/course-docs-smoke.spec.cjs
@@ -19,4 +19,6 @@ test('submissions renders', async ({ page }) => {
   expect(response).not.toBeNull();
   expect(response.ok()).toBeTruthy();
   await expect(page.locator('main').first()).toBeVisible();
+  await expect(page.locator('header').first()).toBeVisible();
+  await expect(page.locator('footer').first()).toBeVisible();
 });

diff --git a/node_modules/@metyatech/course-docs-e2e/tests/e2e/code-preview.spec.cjs b/node_modules/@metyatech/course-docs-e2e/tests/e2e/code-preview.spec.cjs
new file mode 100644
index 0000000..8f4d61e
--- /dev/null
+++ b/node_modules/@metyatech/course-docs-e2e/tests/e2e/code-preview.spec.cjs
@@ -0,0 +1,12 @@
+const { expect, test } = require('@playwright/test');
+
+test('code preview renders editors', async ({ page }) => {
+  const response = await page.goto('/docs/basics/array-intro');
+  if (!response || !response.ok()) {
+    test.skip(true, 'CodePreview sample page not available');
+  }
+
+  const editor = page.locator('.monaco-editor').first();
+  await expect(editor).toBeVisible({ timeout: 30000 });
+  await expect(page.locator('.monaco-editor .view-lines').first()).toContainText('schools');
+});
